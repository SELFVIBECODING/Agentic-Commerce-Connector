export async function runSkillEdit(_args: readonly string[]): Promise<void> {
  process.stdout.write(
    "acc skill edit — not yet implemented (Phase 9+).\n" +
      "Edit ./acc-data/skill/acc-skill.md directly in the meantime.\n",
  );
}
